let unString = 'hola';
let otroString = 'mundo';

let cipher = {
  uno: 1,
  dos: 2,
  tres: 3,
};

window.onload = () => {
  // Terminó de cargar el JS

  // Cuál es el hash que estoy mostrando?
  // => mostrar la sección correspondiente

  if (window.location.pathname.split('#') === )
};
